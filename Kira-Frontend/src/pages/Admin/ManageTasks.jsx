// Deprecated alias to preserve any stray imports; use ManagerTasks instead.
import ManagerTasks from "./ManagerTasks";

const ManageTasks = (props) => <ManagerTasks {...props} />;

export default ManageTasks;
